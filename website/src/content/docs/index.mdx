---
title: Flow
description: Durable workflows for TypeScript that run on the Postgres you already have. A declarative, inspectable DAG — not imperative durable functions.
template: splash
hero:
  tagline: Durable workflows for TypeScript that run on the Postgres you already have. No workflow server, no control plane, no vendor — a library you import, not a platform you adopt.
  actions:
    - text: Quick start
      link: /flow/start/quick-start/
      icon: right-arrow
    - text: How it compares
      link: /flow/start/how-it-compares/
      variant: minimal
    - text: GitHub
      link: https://github.com/octabits-io/flow
      icon: external
      variant: minimal
---

import { Card, CardGrid, Code } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="Declarative, not imperative" icon="seti:config">
    The workflow is a value you can inspect — steps, dependencies and schemas are all
    readable before anything runs. Temporal, Inngest and Trigger.dev take the opposite
    approach: you write a function, and the graph exists only as the trace of what it did.
  </Card>
  <Card title="Typed end to end" icon="seti:typescript">
    Zod schemas per step. A step's dependency outputs arrive typed, and a mismatch is a
    validation failure at runtime rather than a surprise three steps later.
  </Card>
  <Card title="Your Postgres, nothing else" icon="seti:db">
    The store is Postgres and the queue ([pg-boss](https://github.com/timgit/pg-boss)) is
    Postgres too. Nothing else to operate. `import '@octabits-io/flow'` pulls in neither
    `pg`, `pg-boss`, nor the AI SDK — they are optional peers.
  </Card>
  <Card title="The whole durable toolkit" icon="rocket">
    Auto-parallelism, retry and timeout, durable sleep, concurrency caps and rate limits,
    dynamic fan-out, signals, sub-workflows and saga compensation — every transition
    persisted, resumable after a crash.
  </Card>
</CardGrid>

## See it run

A pipeline exercising most of the engine: fan-out over two `map` steps at once, a
concurrency cap, a retried step, a sub-workflow, a suspend-and-resume on an external
event, a durable sleep — then a second workflow failing and rolling back in reverse.

<img src="/flow/demo.svg" alt="Terminal trace of a publishing pipeline running: parallel fan-out with a concurrency cap, a retried step, a sub-workflow, a suspend-and-resume, a durable sleep, then a saga rollback" />

Every line is an engine transition emitted through the
[`FlowObserver`](/flow/running/observability/) seam — not a `console.log` in a handler.

## Install

<Code code={`pnpm add @octabits-io/flow zod`} lang="bash" />

`zod` is a required peer; `pg`, `pg-boss`, `ai` and `@ai-sdk/provider` are optional —
install only what the layers you import need. See [Installation](/flow/start/installation/).
